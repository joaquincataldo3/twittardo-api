import { IModelPaths } from '../interfaces/interfaces';

export const modelPaths: IModelPaths = {
    commentPath: 'comments',
    twittPath: 'twitts',
    userPath: 'user',
    favouritePath: 'favourites',
    twittCommentedPath: 'twittCommented'
}