type SchemaName = {
    user: string,
    admin: string
    twitt: string, 
    comment: string
}

const schemas: SchemaName = {
    user: 'user',
    admin: 'admin',
    twitt: 'twitt',
    comment: 'comment'
}

export default schemas