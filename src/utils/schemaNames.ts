type SchemaName = {
    users: string,
    admins: string
    twitts: string, 
    comments: string
}

const schemas: SchemaName = {
    users: 'users',
    admins: 'admins',
    twitts: 'twitts',
    comments: 'comments'
}

export default schemas